import { Placeholder } from '../ui/Placeholder'

export function AdminView() {
  return (
    <Placeholder
      icono="ti-shield-cog"
      titulo="Administración"
      descripcion="Panel interno para cargar y etiquetar resoluciones de jurisprudencia verificadas (dictámenes DT, oficios SII, sentencias de cortes). En desarrollo."
    />
  )
}
